# Test registrations for the proto track. Owned by the proto track.
#
# Append one add_test(NAME <name> ...) for every test this track adds under
# source/nord/g2/g2Lib/test/. The name is the exact string ctest registers,
# and the string -R must be given to select it. Edit no other CMake file in
# this tree.
