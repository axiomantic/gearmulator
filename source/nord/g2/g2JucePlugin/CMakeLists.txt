cmake_minimum_required(VERSION 3.15)

project(g2JucePlugin VERSION ${CMAKE_PROJECT_VERSION})

# This directory belongs to the PLUGIN track. Every file under it is written by
# a plugin-track task.
# See plan section 7.4.2.
#
# The target is not yet a JUCE plugin. The createJucePlugin() macro that
# every other product in this tree uses takes a product name, a
# four-character plugin code, a binary-data target and a skin, and none of
# those four values is stated anywhere.

add_library(g2JucePlugin STATIC)

set(SOURCES)

# PLG-1 adds the first plugin-track source: the synthLib::Device subclass. The
# tests t0_device_surface and t0_handoff_flags compile this file DIRECTLY (the
# g2PatchLoad.cpp arrangement), so this target's copy and theirs are one
# translation unit, not two.
list(APPEND SOURCES g2Device.cpp)

# PLG-5 adds the seven-item state format the Device's overrides delegate to.
# The same tests compile it directly alongside g2Device.cpp, for the same
# reason.
list(APPEND SOURCES g2State.cpp)

# PLG-8 adds the synthLib::Plugin subclass that sets the resampler mode
# (design section 14.2.2). t0_resampler_mode compiles it directly, the same
# arrangement.
list(APPEND SOURCES g2Plugin.cpp)

# The plugin track appends its own sources here. Until it does, the target
# carries one translation unit GENERATED INTO THE BUILD TREE, for the same
# reason g2Lib does.
if(NOT SOURCES)
	set(G2_JUCE_PLUGIN_PLACEHOLDER ${CMAKE_CURRENT_BINARY_DIR}/g2JucePluginPlaceholder.cpp)
	file(WRITE ${G2_JUCE_PLUGIN_PLACEHOLDER}
		"// Generated by g2JucePlugin/CMakeLists.txt until the plugin track adds a source.\n"
		"namespace g2 { extern const int g2JucePluginPlaceholder; const int g2JucePluginPlaceholder = 0; }\n")
	list(APPEND SOURCES ${G2_JUCE_PLUGIN_PLACEHOLDER})
endif()

target_sources(g2JucePlugin PRIVATE ${SOURCES})
source_group("source" FILES ${SOURCES})

target_link_libraries(g2JucePlugin PUBLIC g2Lib)

set_property(TARGET g2JucePlugin PROPERTY FOLDER "G2")
